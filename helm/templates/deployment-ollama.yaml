apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-ollama
  namespace: {{ .Release.Namespace }}
  labels:
    app: deployment-ollama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deployment-ollama
  template:
    metadata:
      labels:
        app: deployment-ollama
    spec:
      volumes:
      - name: ollama-fs
        persistentVolumeClaim:
          claimName: ollama-fs
      runtimeClassName: nvidia
      containers:
      - name: ollama
        image: docker.io/ollama/ollama:0.31.1
        env:
        - name: TZ
          value: "America/Denver"
        - name: OLLAMA_DEBUG
          value: "1"
        resources:
          limits:
            memory: 6G
            cpu: 2
            nvidia.com/gpu: 2
        ports:
        - containerPort: 11434
        volumeMounts:
        - mountPath: /root/.ollama
          name: ollama-fs
